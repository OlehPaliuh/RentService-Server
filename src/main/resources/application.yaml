management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
spring:
  boot:
    admin:
      client:
        url: [http://localhost:8080]
      ui:
        title: MaklerTaboo monitoring
      notify:
        mail:
          enabled: true
          to: pryzygley@gmail.com
  application:
    name: Rent Service Server Application

